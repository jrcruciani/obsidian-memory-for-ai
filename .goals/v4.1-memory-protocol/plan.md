# Implementation plan

1. Read the baseline, run gates, copy the v4 vault/spec, record decisions.
2. Implement temporal facts and lineage/trust with transactional regression tests.
3. Implement aliases/ranked retrieval and deterministic budgeted bootstrap.
4. Add staleness and consolidation through the proposal lifecycle.
5. Add JSON interfaces, agent guidance, evaluations, and CI.
6. Finish documentation and run all gates before the final commit.

Each phase ends with the full quality gate and a separate commit.
